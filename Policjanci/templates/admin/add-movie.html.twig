{% extends 'base.html.twig' %}

{% block title %}Add Movie – PlusFlix{% endblock %}

{% block body %}

    <h1>Add a Movie</h1>

    {{ form_start(form, {
        action: path('admin_add_movie_submit'),
        method: 'POST'
    }) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">
        Add Movie
    </button>

    {{ form_end(form) }}

{% endblock %}
